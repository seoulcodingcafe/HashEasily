<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.hasheazy">

    <application
        android:name="io.github.hasheazy.ClassApplication"
        android:allowBackup="true"
        android:icon="@drawable/launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cyanea.Light.DarkActionBar">
        <activity
            android:name="io.github.hasheazy.LicenseActivity"
            android:label="@string/license"
            android:theme="@style/NoActionBar" />
        <activity
            android:name="io.github.hasheazy.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>